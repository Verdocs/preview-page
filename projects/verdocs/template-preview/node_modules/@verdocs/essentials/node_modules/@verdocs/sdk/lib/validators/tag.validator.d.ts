import { AbstractControl, ValidatorFn, FormControl, FormGroupDirective, NgForm } from '@angular/forms';
import { ErrorStateMatcher } from '@angular/material/core';
export declare class TagValidator {
    static MatchTag(control: AbstractControl): {
        textPattern: boolean;
    };
}
export declare function TagDuplicateValidator(tags: any[]): ValidatorFn;
export declare class TagErrorStateMatcher implements ErrorStateMatcher {
    isErrorState(control: FormControl | null, form: FormGroupDirective | NgForm | null): boolean;
}
