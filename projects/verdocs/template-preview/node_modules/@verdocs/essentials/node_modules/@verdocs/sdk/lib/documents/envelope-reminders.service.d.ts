import { Injector } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { IReminder } from '../models/reminder.model';
export declare class EnvelopRemindersService {
    private http;
    private injector;
    private _config_token;
    private _rForm_backend_url;
    constructor(http: HttpClient, injector: Injector);
    getReminder(envelopeId: any, reminderId: any): Promise<IReminder>;
    updateReminder(envelopeId: string, reminderId: any, body: any): Promise<IReminder>;
    deleteReminder(envelopeId: any, reminderId: any): Promise<Object>;
    createReminder(envelopeId: any, body: {
        setup_time: number;
        interval_time: number;
        is_on: boolean;
    }): Promise<IReminder>;
}
