import { Injector } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { IReminder } from '../models/reminder.model';
export declare class TemplatesRemindersService {
    private http;
    private injector;
    private _config_token;
    private _rForm_backend_url;
    constructor(http: HttpClient, injector: Injector);
    getReminder(templateId: any, reminderId: any): Promise<IReminder>;
    updateReminder(templateId: string, reminderId: any, body: any): Promise<IReminder>;
    deleteReminder(templateId: any, reminderId: any): Promise<Object>;
    createReminder(templateId: any, body: {
        setup_time: number;
        interval_time: number;
    }): Promise<IReminder>;
}
