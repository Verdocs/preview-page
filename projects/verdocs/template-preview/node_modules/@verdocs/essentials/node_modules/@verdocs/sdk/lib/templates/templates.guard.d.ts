import { VerdocsTokenObjectService } from '@verdocs/tokens';
import { TemplateActions } from '../definitions/template.enums';
import { ITemplate } from '../models/template.model';
export declare class TemplatesGuardService {
    private vTokenObjectService;
    constructor(vTokenObjectService: VerdocsTokenObjectService);
    canPerformAction(action: TemplateActions, template: ITemplate): {
        canPerform: boolean;
        message: string;
    };
    private hasPermissions;
    canBeSender(template: any): boolean;
    canUserCreateTemplate(): boolean;
}
