import { MatDialog } from '@angular/material/dialog';
export declare class ErrorDialogService {
    private dialog;
    constructor(dialog: MatDialog);
    show401ResponseError(err: any): void;
    showPlansError(type: string, redirect_url: string, dialog_title?: string, dialog_message?: string): void;
}
