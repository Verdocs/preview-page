import { Injector } from '@angular/core';
import { MatDialog } from '@angular/material/dialog';
import { VerdocsStateService } from '@verdocs/tokens';
export declare class GuardService {
    private injector;
    private stateService;
    private dialog;
    private platform;
    private _config_token;
    private _plans;
    private _rAccount_frontend_url;
    private _redirectUrl;
    constructor(injector: Injector, stateService: VerdocsStateService, dialog: MatDialog, platform: any);
    checkSubscription(type: string, subscriptionType: string, openDialog: boolean): boolean;
    toTokenObject(accessToken: any): any;
    getHighestEnvelopePlan(plans: any): any;
}
