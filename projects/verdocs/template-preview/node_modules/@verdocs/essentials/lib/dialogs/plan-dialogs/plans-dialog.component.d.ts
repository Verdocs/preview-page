import { OnInit } from '@angular/core';
import { Router } from '@angular/router';
import { MatDialogRef } from '@angular/material/dialog';
export declare class PlansDialog implements OnInit {
    private router;
    private dialog;
    redirectUrl: string;
    type: string;
    dialogTitle: string;
    dialogMessage: string;
    constructor(router: Router, dialog: MatDialogRef<PlansDialog>);
    ngOnInit(): void;
    goToBilling(): void;
    close(): void;
}
