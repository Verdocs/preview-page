import { OnInit, Injector } from '@angular/core';
import { VerdocsTokenObjectService } from '@verdocs/tokens';
import { Router } from '@angular/router';
export declare class FourOhOneDialog implements OnInit {
    private router;
    private injector;
    private vTokenObjectService;
    error: any;
    currentProfile: any;
    error_code: string;
    error_title: string;
    error_message: string;
    rSecureUrl: string;
    rFormUrl: string;
    private _config_token;
    constructor(router: Router, injector: Injector, vTokenObjectService: VerdocsTokenObjectService);
    ngOnInit(): void;
    prepareErroContent(): void;
    goToDashboard(): void;
    signIn(): void;
    signUp(): void;
}
