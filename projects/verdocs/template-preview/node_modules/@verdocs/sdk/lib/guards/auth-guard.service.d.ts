import { CanActivate, CanActivateChild, CanLoad, ActivatedRouteSnapshot, RouterStateSnapshot, Route } from '@angular/router';
import { VerdocsAuthService, VerdocsAuthGuardService, VerdocsStateService } from '@verdocs/tokens';
export declare class AuthGuardService implements CanActivate, CanActivateChild, CanLoad {
    private vTokenAuth;
    private vTokenAuthGuard;
    private vTokenStateService;
    private document;
    constructor(vTokenAuth: VerdocsAuthService, vTokenAuthGuard: VerdocsAuthGuardService, vTokenStateService: VerdocsStateService, document: any);
    canActivate(route: any, state: RouterStateSnapshot): boolean;
    canActivateChild(route: ActivatedRouteSnapshot, state: RouterStateSnapshot): boolean;
    checkLogin(route: any, url: any): boolean;
    canLoad(route: Route): boolean;
    canLoadBuilder(route: ActivatedRouteSnapshot, state: RouterStateSnapshot): void;
    isGuestLink(route: any): boolean;
}
