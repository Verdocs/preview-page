import { Injector } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { ReplaySubject } from 'rxjs';
import { IRecipient } from '../models/recipient.model';
export declare class RecipientService {
    private httpClient;
    private injector;
    recipientsSubject: ReplaySubject<IRecipient[]>;
    private _config_token;
    private _rForm_backend_url;
    private recipients;
    constructor(httpClient: HttpClient, injector: Injector);
    private updateRecipients;
    getRecipients(envelopeId: any): Promise<any[] | IRecipient[]>;
    prepareRecipients(envId: string, roleName: string, recipientsToUpdate: any[]): Promise<void>;
    updateRecipientName(envId: string, roleName: string, newFullName: string): Promise<void>;
    setAsAgreed(envId: string, roleName: string): Promise<void>;
    claimProfile(envId: string, roleName: string, profileToClaim: any): Promise<void>;
    updateRecipientInfoByOwner(envelopeId: string, roleName: string, recipientInfo: {
        full_name: string;
        email: string;
        phone: string;
    }): Promise<IRecipient>;
    getInPersonLink(envelopeId: any, roleName: string): Promise<Object>;
}
