import { OnChanges } from '@angular/core';
import { VerdocsTokenObjectService } from '@verdocs/tokens';
import { VerdocsHeaderService } from '../../services/header.service';
import { ITemplate } from '../../models/verdocs.model';
export declare class RecipientEditorComponent implements OnChanges {
    private verdocsHeaderService;
    private vTokenObjectService;
    roles: any[];
    sender: string;
    sender_name: string;
    template: ITemplate;
    constructor(verdocsHeaderService: VerdocsHeaderService, vTokenObjectService: VerdocsTokenObjectService);
    ngOnChanges(): void;
    prepareSender(): Promise<void>;
    updateSenderName(): Promise<void>;
    prepareExistingRoles(recipients?: any): void;
    setStyles(roles: any): any;
}
