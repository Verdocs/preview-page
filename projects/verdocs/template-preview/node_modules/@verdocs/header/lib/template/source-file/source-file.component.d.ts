import { OnChanges, SimpleChanges } from '@angular/core';
import { VerdocsHeaderService } from '../../services/header.service';
import { ITemplate } from '../../models/verdocs.model';
export declare class TemplateSourceComponent implements OnChanges {
    private templatesService;
    template: ITemplate;
    constructor(templatesService: VerdocsHeaderService);
    ngOnChanges(changes: SimpleChanges): void;
    downloadTemplateDocument(): void;
}
