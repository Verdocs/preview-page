import { OnInit, Injector } from '@angular/core';
import { VerdocsTokenObjectService } from '@verdocs/tokens';
import { Router } from '@angular/router';
import { connectionConfig } from '../header.module';
export declare class FourOhOneDialog implements OnInit {
    private router;
    private verdocsTokenObjectService;
    private injector;
    error: any;
    tokenConfig: connectionConfig;
    currentProfile: any;
    error_code: string;
    error_title: string;
    error_message: string;
    rSecureUrl: string;
    rFormUrl: string;
    constructor(router: Router, verdocsTokenObjectService: VerdocsTokenObjectService, injector: Injector);
    ngOnInit(): void;
    prepareErroContent(): void;
    goToDashboard(): void;
    signIn(): void;
    signUp(): void;
}
