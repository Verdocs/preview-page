import { Injector } from '@angular/core';
import { HttpRequest, HttpHandler, HttpInterceptor, HttpResponse } from '@angular/common/http';
export declare class VerdocsResponseInterceptor implements HttpInterceptor {
    private injector;
    private stateService;
    constructor(injector: Injector);
    intercept(request: HttpRequest<any>, next: HttpHandler): import("rxjs").Observable<HttpResponse<any>>;
}
