import { InjectionToken } from '@angular/core';
export interface VTokenConfig {
    production: boolean;
    domain: string;
    cookieName: string;
    loginURL: string;
    verdocs_name?: string;
    account_name?: string;
    origin?: 'rAccount' | 'rForm';
}
export declare const VTokenConfigToken: InjectionToken<VTokenConfig>;
export declare class TokensModule {
    static initInterceptor(config: VTokenConfig): {
        ngModule: typeof TokensModule;
        providers: {
            provide: InjectionToken<VTokenConfig>;
            useValue: VTokenConfig;
        }[];
    };
}
