import { ActivatedRoute } from '@angular/router';
import { VerdocsAuthService } from './auth.service';
import { VerdocsStateService } from './state.service';
export declare class VerdocsAuthGuardService {
    private authService;
    private stateService;
    private route;
    private signOutRequestSubmitted;
    constructor(authService: VerdocsAuthService, stateService: VerdocsStateService, route: ActivatedRoute);
    checkLogin(): void;
    checkAuthentication(): void;
    signOut(redirectUrl?: string): void;
}
