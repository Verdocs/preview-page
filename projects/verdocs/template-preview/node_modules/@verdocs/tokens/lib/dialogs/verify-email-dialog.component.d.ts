import { OnInit } from '@angular/core';
import { MatDialogRef } from '@angular/material';
import { VerdocsAuthGuardService } from '../services/authguard.service';
import { VerdocsStateService } from '../services/state.service';
import { EmailVerificationData } from '../models/email-verification-data.model';
export declare class VerifyEmailDialogComponent implements OnInit {
    private dialogRef;
    private vTokenAuthGuard;
    private vTokenStateService;
    data: EmailVerificationData;
    constructor(dialogRef: MatDialogRef<VerifyEmailDialogComponent>, vTokenAuthGuard: VerdocsAuthGuardService, vTokenStateService: VerdocsStateService, data: EmailVerificationData);
    ngOnInit(): void;
    signOut(): void;
    resendEmailVerification(): void;
    closeDialog(): void;
}
